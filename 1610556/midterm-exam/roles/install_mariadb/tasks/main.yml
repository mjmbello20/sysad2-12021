---
# tasks file for roles/install_mariadb
  - name: mariadb installation
    apt:
      name: mariadb-server-10.3
      state: present
      update_cache: yes

  - name: Starting mariadb
    service:
      name: mariadb
      state: started
